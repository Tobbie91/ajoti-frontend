import { useEffect,useState } from 'react'
import { Button,Card,Group,Modal,NumberInput,Progress,SegmentedControl,Select,Stack,Switch,Text,TextInput,Textarea,Title } from '@mantine/core'
import { IconPlus,IconUsers } from '@tabler/icons-react'
import { contributeTargetSavings,createTargetSavings,getMyTargetSavings,getPublicTargetSavings,joinTargetSavings,type TargetSavingsPlan } from '@/utils/targetSavingsApi'
const toNaira=(k:string)=>Number(k||0)/100;const money=(k:string)=>`₦${toNaira(k).toLocaleString('en-NG',{minimumFractionDigits:2})}`
export function TargetSavings(){const[plans,setPlans]=useState<TargetSavingsPlan[]>([]);const[pub,setPub]=useState<TargetSavingsPlan[]>([]);const[open,setOpen]=useState(false);const[busy,setBusy]=useState(false);const[error,setError]=useState('');const[form,setForm]=useState({type:'INDIVIDUAL',name:'',description:'',targetAmount:100000,contributionAmount:10000,frequency:'MONTHLY',maturityDate:'',isPublic:false});const load=()=>Promise.all([getMyTargetSavings(),getPublicTargetSavings()]).then(([m,p])=>{setPlans(m);setPub(p.filter(x=>!m.some(y=>y.id===x.id)))});useEffect(()=>{load().catch(()=>{})},[]);const create=async()=>{setBusy(true);setError('');try{await createTargetSavings({type:form.type as 'INDIVIDUAL'|'GROUP',name:form.name,description:form.description||undefined,targetAmountKobo:String(Math.round(form.targetAmount*100)),contributionAmountKobo:String(Math.round(form.contributionAmount*100)),frequency:form.frequency as 'DAILY'|'WEEKLY'|'MONTHLY',startDate:new Date().toISOString(),maturityDate:new Date(`${form.maturityDate}T23:59:59`).toISOString(),isPublic:form.type==='GROUP'?form.isPublic:false});setOpen(false);await load()}catch(e){setError(e instanceof Error?e.message:'Unable to create target')}finally{setBusy(false)}};return <div className="mx-auto w-full max-w-[1100px] px-4 py-6"><Group justify="space-between" mb="lg"><div><Title order={2}>Target Savings</Title><Text c="dimmed">Save towards your own goal or stay accountable with a group.</Text></div><Button leftSection={<IconPlus size={16}/>} onClick={()=>setOpen(true)}>New target</Button></Group><Stack gap="md">{plans.length===0&&<Card withBorder><Text fw={600}>No savings targets yet</Text><Text size="sm" c="dimmed">Create an individual target or start a group target.</Text></Card>}{plans.map(p=><TargetCard key={p.id} plan={p} onChanged={load}/>)}{pub.length>0&&<><Title order={3} mt="lg">Public group targets</Title>{pub.map(p=><Card key={p.id} withBorder><Group justify="space-between"><div><Text fw={700}>{p.name}</Text><Text size="sm" c="dimmed">{p.memberCount} members · {money(p.contributionAmountKobo)} {p.frequency.toLowerCase()}</Text></div><Button variant="light" leftSection={<IconUsers size={16}/>} onClick={()=>joinTargetSavings(p.id).then(load)}>Join</Button></Group></Card>)}</>}</Stack><Modal opened={open} onClose={()=>setOpen(false)} title="Create savings target" centered><Stack><SegmentedControl value={form.type} onChange={type=>setForm({...form,type})} data={[{label:'Individual',value:'INDIVIDUAL'},{label:'Group',value:'GROUP'}]}/><TextInput label="Target name" value={form.name} onChange={e=>setForm({...form,name:e.currentTarget.value})}/><Textarea label="Description (optional)" value={form.description} onChange={e=>setForm({...form,description:e.currentTarget.value})}/><NumberInput label="Your target amount (₦)" min={1} value={form.targetAmount} onChange={v=>setForm({...form,targetAmount:Number(v)||0})} thousandSeparator=","/><NumberInput label="Amount to save each time (₦)" min={1} value={form.contributionAmount} onChange={v=>setForm({...form,contributionAmount:Number(v)||0})} thousandSeparator=","/><Select label="How often?" value={form.frequency} onChange={v=>setForm({...form,frequency:v||'MONTHLY'})} data={[{value:'DAILY',label:'Daily'},{value:'WEEKLY',label:'Weekly'},{value:'MONTHLY',label:'Monthly'}]}/><TextInput type="date" label="Maturity date" value={form.maturityDate} onChange={e=>setForm({...form,maturityDate:e.currentTarget.value})}/>{form.type==='GROUP'&&<Switch label="Make this group public" checked={form.isPublic} onChange={e=>setForm({...form,isPublic:e.currentTarget.checked})}/>} {error&&<Text c="red" size="sm">{error}</Text>}<Button loading={busy} disabled={!form.name||!form.maturityDate} onClick={create}>Create target</Button></Stack></Modal></div>}
function TargetCard({plan,onChanged}:{plan:TargetSavingsPlan;onChanged:()=>Promise<unknown>}){const[amount,setAmount]=useState(toNaira(plan.contributionAmountKobo));const mine=plan.myMembership;return <Card withBorder radius="md" p="lg"><Group justify="space-between" align="flex-start"><div><Text fw={700} fz="lg">{plan.name}</Text><Text size="sm" c="dimmed">{plan.type==='GROUP'?`${plan.memberCount} members · Group accountability`:'Individual target'} · {plan.frequency.toLowerCase()}</Text></div><Text size="sm" fw={600}>{plan.status}</Text></Group><Progress mt="md" value={mine?.progressPercent??0}/><Group justify="space-between" mt="xs"><Text size="sm">{money(mine?.savedAmountKobo??'0')} saved</Text><Text size="sm">Goal {money(plan.targetAmountKobo)}</Text></Group>{plan.status==='ACTIVE'&&mine&&<Group mt="md"><NumberInput min={1} value={amount} onChange={v=>setAmount(Number(v)||0)} prefix="₦" thousandSeparator="," style={{flex:1}}/><Button onClick={()=>contributeTargetSavings(plan.id,String(Math.round(amount*100))).then(onChanged)}>Save now</Button></Group>}{plan.type==='GROUP'&&plan.inviteToken&&<Text mt="md" size="xs" c="dimmed">Invite token: {plan.inviteToken}</Text>}{plan.type==='GROUP'&&<Stack gap={4} mt="md">{plan.members.slice().sort((a,b)=>b.progressPercent-a.progressPercent).map((m,i)=><Group key={m.id} justify="space-between"><Text size="sm">{i+1}. {m.user.firstName} {m.user.lastName}</Text><Text size="sm">{m.progressPercent.toFixed(0)}%</Text></Group>)}</Stack>}</Card>}
